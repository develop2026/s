name: 使用 GitHub CLI 设置 Secrets

on:
  workflow_dispatch:

jobs:
  manage-secrets:
    runs-on: ubuntu-latest

    steps:
    - name: 安装 GitHub CLI
      uses: actions/setup-gh@v3
      with:
        version: 'latest'